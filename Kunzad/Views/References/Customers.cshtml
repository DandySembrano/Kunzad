@{
    Layout = "~/Views/Shared/_DataTableLayout4.cshtml";
}

<!-- Customer List/DataTable -->
@section tableContent{
    <div class="row">
        <div class="col-lg-12">
            <table class="table table-hover">
                <thead class="text-dark">
                    <tr>
                        <th class="col-md-1 small"><strong>No</strong></th>
                        <th class="col-md-1 small"><strong>Code</strong></th>
                        <th class="col-md-3 small"><strong>Name</strong></th>
                        <th class="col-md-3 small"><strong>Industry</strong></th>
                        <th class="col-md-2 small"><strong>TIN</strong></th>
                    </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="c in data" ng-click="setSelected($index)"
                        context-menu="setSelected($index)" data-target="DataTableMenu">
                        <td class="small">{{currentPage == 1?($index + 1) : (($index + 1) + ((currentPage -1) * pageSize) )}}</td>
                        <td class="small">{{c.Code}}</td>
                        <td class="small">{{c.Name}}</td>
                        <td class="small" ng-repeat="indust in Industries | filter:{Id : c.IndustryId}">{{indust.Name}}</td>
                        <td class="small">{{c.TIN}}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
}

<!-- Customer Form -->
@section tab1Content{
    <div class="row" ng-show="selectedTab === tabPages[0]">
        <div class="col-lg-6">
            <div class="alert alert-danger alert-sm" ng-show="isError">
                <i class="fa fa-remove pr10"></i>
                <strong>ERROR!</strong>    {{errorMessage}}
            </div>
            <form class="form-horizontal" role="form">
                <div class="form-group form-group-sm">
                    <label class="col-md-3 control-label small">Code</label>
                    <div class="col-md-4">
                        <input type="text" class="form-control" placeholder=""
                               ng-model="dataItem.Code" ng-disabled="viewOnly">
                    </div>
                </div>

                <div class="form-group form-group-sm">
                    <label class="col-md-3 control-label small">Name</label>
                    <div class="col-md-8">
                        <input type="text" class="form-control" placeholder=""
                               ng-model="dataItem.Name" ng-disabled="viewOnly">
                    </div>
                </div>

                <div class="form-group form-group-sm">
                    <label class="col-md-3 control-label small">Customer Group</label>
                    <div class="col-md-8">
                        <select class="form-control" placeholder="" ng-disabled="viewOnly"
                                ng-change="selectCustomerGroup(dataItem.CustomerGroupId)"
                                ng-model="dataItem.CustomerGroupId" ng-options="cg.Id as cg.Name for cg in CustomerGroups"></select>
                    </div>
                </div>

                <div class="form-group form-group-sm">
                    <label class="col-md-3 control-label small">Industry</label>
                    <div class="col-md-8">
                        <select class="form-control" placeholder="" ng-disabled="viewOnly"
                                ng-change="selectIndustry(dataItem.IndustryId)"
                                ng-model="dataItem.IndustryId" ng-options="item.Id as item.Name for item in Industries"></select>
                    </div>
                </div>

                <div class="form-group form-group-sm">
                    <label class="col-md-3 control-label small">TIN</label>
                    <div class="col-md-8">
                        <input type="text" class="form-control" placeholder=""
                               ng-model="dataItem.TIN" ng-disabled="viewOnly">
                    </div>
                </div>

            </form>
        </div>
    </div>
}

<!-- Customer Addresses -->
@section tab2Content{
    <div class="row" ng-show="selectedTab === tabPages[1]">
        <div class="col-lg-12">

            <h2 class="mt25 lh35 mtn text-left">
                <b>{{dataItem.Name}}</b>
            </h2>
            <button class="btn btn-sm pull-right" ng-click="openCustomerAddressForm('Create')">
                <span class="glyphicon glyphicon-plus"></span>
            </button>
            <div class="row" ng-repeat="item in customerAddressList">
                <div class=""></div>
                <div class="col-lg-8">
                    <div class="well">
                        <div class="row">
                            <div class="col-md-12" align="right">
                                <button class="btn btn-xs btn-primary light" type="button" ng-click="openCustomerAddressForm('Edit', $index)">
                                    <span class="glyphicon glyphicon-pencil"></span>
                                </button>
                                <button class="btn btn-xs btn-danger light" type="button" ng-click="openCustomerAddressForm('Delete', $index)">
                                    <span class="glyphicons glyphicons-bin"></span>
                                </button>
                            </div>
                        </div>
                        <address>
                            <strong>{{dataItem.Name}}</strong>
                            <span><br>{{item.Line1}}</span>
                            <span ng-show="item.Line2 != null && item.Line2 != ''"><br>{{item.Line2}}</span>
                            <span><br>{{item.CityMunicipality.Name + ", " + item.CityMunicipality.StateProvince.Name + " " + item.PostalCode }}</span>
                            <span><br>{{country.Name}}</span>
                        </address>
                        <div class="row">
                            <div class="col-lg-6">
                                <div class="checkbox-custom checkbox-disabled">
                                    <input id="checkbox1" type="checkbox" ng-model="item.IsPickupAddress" disabled="">
                                    <label for="checkbox1" class="small">Pickup Address</label>
                                </div>
                                <div class="checkbox-custom checkbox-disabled">
                                    <input id="checkbox2" type="checkbox" ng-model="item.IsDeliveryAddress" disabled="">
                                    <label for="checkbox2" class="small">Delivery Address</label>
                                </div>
                            </div>
                            <div class="col-lg-6">
                                <div class="checkbox-custom checkbox-disabled">
                                    <input id="checkbox3" type="checkbox" ng-model="item.IsBillingAddress" disabled="">
                                    <label for="checkbox3" class="small">Billing Address</label>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <button class="btn btn-sm pull-right" ng-click="initAddress(); openModalForm('#modal-panel-address');"
                    ng-show="dataItem.CustomerAddresses.length > 1"></button>
        </div>
    </div>
}

<!-- Contacts -->
@section tab3Content{
    <div class="row" ng-show="selectedTab === tabPages[2]">
        <!--contact list-->
        <div class="row" ng-show="!showSubForm">
            <div class="col-md-12">
                <div class="panel mb25 mt5">
                    <div class="panel-body pn">
                        <div class="col-lg-12">
                            <table class="table table-hover">
                                <thead class="text-dark">
                                    <tr>
                                        <th class="col-md-1 small"><strong>Name</strong></th>
                                        <th class="col-md-3 small"><strong>Title</strong></th>
                                        <th class="col-md-3 small"><strong>Email</strong></th>
                                        <th class="col-md-2 small"><strong>Alternate Email</strong></th>
                                    </tr>
                                </thead>
                                <tbody>

                                    <tr ng-repeat="c in customerContactList" ng-click="setSubSelected($index)"
                                        context-menu="setSubSelected($index)" data-target="DataTableMenu2">
                                        <td class="small">{{c.Contact.Name}}</td>
                                        <td class="small">{{c.Contact.Title}}</td>
                                        <td class="small">{{c.Contact.Email}}</td>
                                        <td class="small">{{c.Contact.AlternateEmail}}</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                    <div class="panel-footer">
                        <div class="row">
                            <div class="col-md-6" align="left">
                                <button class="btn btn-sm btn-primary" type="button" ng-click="actionSubForm('Create')">Create</button>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!--General,Contacts,PHones-->
        <div class="row" ng-show="showSubForm">
            <div class="col-md-12">
                <div class="">
                    <div class="alert alert-danger alert-sm" ng-show="isError">
                        <i class="fa fa-remove pr10"></i>
                        <strong>ERROR!</strong>    {{errorMessage}}
                    </div>
                    <!-- contact -->
                    <div class="row" ng-show="selectedSubTab === subTabPages[0]">
                        <h2 class="header-lg">
                            <b>{{dataItem.Name}}</b>
                        </h2>

                        <div class="col-lg-6">
                            <form class="form-horizontal" role="form">
                                <div class="form-group form-group-sm">

                                    <label class="col-md-3 control-label small">Name</label>
                                    <div class="col-md-8">
                                        <input type="text" class="form-control" placeholder=""
                                               ng-model="contact.Name" ng-disabled="viewOnly">
                                    </div>
                                </div>
                                <div class="form-group form-group-sm">
                                    <label class="col-md-3 control-label small">Title</label>
                                    <div class="col-md-8">
                                        <input type="text" class="form-control" placeholder=""
                                               ng-model="contact.Title" ng-disabled="viewOnly">
                                    </div>
                                </div>
                                <div class="form-group form-group-sm">
                                    <label class="col-md-3 control-label small">Email</label>
                                    <div class="col-md-8">
                                        <input type="text" class="form-control" placeholder=""
                                               ng-model="contact.Email" ng-disabled="viewOnly">
                                    </div>
                                </div>
                                <div class="form-group form-group-sm">
                                    <label class="col-md-3 control-label small">Alternate Email</label>
                                    <div class="col-md-8">
                                        <input type="text" class="form-control" placeholder=""
                                               ng-model="contact.AlternateEmail" ng-disabled="viewOnly">
                                    </div>
                                </div>
                            </form>
                        </div>
                        <!-- contact phone-->

                        <div class="col-lg-12">
                            <button class="btn btn-sm pull-right" ng-click="openCustomerContactPhoneForm('Create')">
                                <span class="glyphicon glyphicon-plus"></span>
                            </button>
                            </br>
                            </br>
                            <div class="panel-heading">
                                <span class="panel-title">
                                    <span class="glyphicons glyphicons-table"></span>
                                    Contact Phone
                                </span>
                            </div>
                            <div class="panel-body pn">
                                <table class="table">
                                    <thead>
                                        <tr>
                                            <th class="col-md-2 small"><strong>Contact Number</strong></th>
                                            <th class="col-md-2 small"><strong>Type</strong></th>
                                            <th class="small text-right"><strong>Edit/Delete</strong></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr ng-repeat="c in customerContactPhoneList"
                                            context-menu="setSelected($index)"  data-target="DataTableMenu2">
                                            <td class="small">{{c.ContactNumber}}</td>
                                                <td class="small" ng-repeat="cc in CustomerContactTypes | filter:{Id : c.ContactNumberTypeId} ">{{cc.Type}}</td>
                                            
                                                <td class="small text-right">
                                                    <button class="btn btn-xs btn-primary light" type="button" ng-click="openCustomerContactPhoneForm('Edit', $index)">
                                                        <span class="glyphicon glyphicon-pencil"></span>
                                                    </button>
                                                    <button class="btn btn-xs btn-danger light" type="button" ng-click="openCustomerContactPhoneForm('Delete', $index)">
                                                        <span class="glyphicons glyphicons-bin"></span>
                                                    </button>
                                                </td>
                                            </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <br />
        <div ng-show="showSubFooter">
            <div class="panel-footer">
                <div class="row">
                    <div class="col-md-6" align="left">
                        <button class="btn btn-sm btn-primary" type="submit" ng-click="closeSubForm()">Back</button>
                    </div>
                    <div class="col-md-6" align="right">
                        <button class="btn btn-sm btn-primary" type="submit" ng-click="submitSubForm()"
                                ng-show="actionMode != 'View'">
                            {{saveContactText}}
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

<!-- This is the template use for City/Municpality Typeahead -->
<script type="text/ng-template" id="customTemplate.html">
    <a>
        <span bind-html-unsafe="match.label | typeaheadHighlight:query"></span>
        <span>, {{match.model.StateProvinceName + ", " + match.model.CountryName}}</span>
    </a>
</script>


<!-- Admin Form Popup for Customer Address -->
<div id="modal-panel-address" class="popup-basic bg-none mfp-with-anim mfp-hide">
    <div class="panel">
        <div class="panel-heading">
            <span class="panel-title"><i class="glyphicon glyphicon-tasks"></i>{{customerAddressAction}} Customer Address</span>
        </div>
        <div class="panel-body">
            <div class="alert alert-danger alert-sm" ng-show="isError">
                <i class="fa fa-remove pr10"></i>
                <strong>ERROR!</strong>    {{errorMessage}}
            </div>
            <div class="row">
                <div class="col-lg-12">
                    <form class="form-horizontal" role="form">

                        <div class="form-group form-group-sm">
                            <label class="col-md-3 control-label small">Street Address</label>
                            <div class="col-md-8">
                                <input type="text" class="form-control" placeholder="Street address line 1"
                                       ng-model="customerAddress.Line1" ng-disabled="viewOnly">
                            </div>
                        </div>

                        <div class="form-group form-group-sm">
                            <label class="col-md-3 control-label small"></label>
                            <div class="col-md-8">
                                <input type="text" class="form-control" placeholder="Street address line 2"
                                       ng-model="customerAddress.Line2" ng-disabled="viewOnly">
                            </div>
                        </div>

                        <div class="form-group form-group-sm">
                            <label class="col-md-3 control-label small">City/Municipality</label>
                            <div class="col-md-8">
                                <input type="text" class="form-control" placeholder="" ng-disabled="viewOnly"
                                       ng-model="customerAddress.CityMunicipality.Name" typeahead-editable="false"
                                       typeahead-on-select="onSelectCity($item, $model, $label)"
                                       typeahead="city.Name for city in cityMunicipalities | filter:$viewValue | limitTo:12"
                                       typeahead-template-url="customTemplate.html">
                            </div>
                        </div>

                        <div class="form-group form-group-sm">
                            <label class="col-md-3 control-label small">Province/State</label>
                            <div class="col-md-8">
                                <input type="text" class="form-control" placeholder=""
                                       ng-model="customerAddress.CityMunicipality.StateProvince.Name" ng-disabled="true">
                            </div>
                        </div>

                        <div class="form-group form-group-sm">
                            <label class="col-md-3 control-label small">Postal Code</label>
                            <div class="col-md-8">
                                <input type="text" class="form-control" placeholder=""
                                       ng-model="customerAddress.PostalCode" ng-disabled="viewOnly">
                            </div>
                        </div>

                        <div class="form-group form-group-sm">
                            <label class="col-md-3 control-label small">Country</label>
                            <div class="col-md-8">
                                <input type="text" class="form-control" placeholder=""
                                       ng-model="country.Name" ng-disabled="true">
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="checkbox-custom mb5">
                                <input id="checkbox1" type="checkbox" ng-model="customerAddress.IsPickupAddress">
                                <label for="checkbox1" class="small">Pickup Address</label>
                            </div>

                            <div class="checkbox-custom mb5">
                                <input id="checkbox2" type="checkbox" ng-model="customerAddress.IsDeliveryAddress">
                                <label for="checkbox2" class="small">Delivery Address</label>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="checkbox-custom mb5">
                                <input id="checkbox3" type="checkbox" ng-model="customerAddress.IsBillingAddress">
                                <label for="checkbox3" class="small">Billing Address{{customerAddress.IsBillingAddress}}</label>
                            </div>
                        </div>
                    </form>
                </div>

            </div>
        </div>
        <div class="panel-footer text-right">
            <button class="btn btn-primary" type="submit"
                    ng-click="saveCustomerAddress(customerAddressAction)">
                {{customerAddressAction === 'Create' ? 'Add' : customerAddressAction}}
            </button>

        </div>
    </div>
</div>

<!--panel for contact phone-->
<div id="modal-panel-contactphone" class="popup-basic bg-none mfp-with-anim mfp-hide">
    <div class="panel">
        <div class="panel-heading">
            <span class="panel-title"><i class="glyphicon glyphicon-tasks"></i>{{ContatPhoneAction}} Contact Phone</span>
        </div>
        <div class="panel-body">
            <div class="alert alert-danger alert-sm" ng-show="isError">
                <i class="fa fa-remove pr10"></i>
                <strong>ERROR!</strong>    {{errorMessage}}
            </div>
            <div class="row">
                <div class="col-lg-12">
                    <form class="form-horizontal" role="form">

                        <div class="form-group form-group-sm">
                            <label class="col-md-3 control-label small">Contact No</label>
                            <div class="col-md-8">
                                <input type="text" class="form-control"
                                       ng-model="customerContactPhone.ContactNumber" ng-disabled="viewOnly">
                            </div>
                        </div>

                        <div class="form-group form-group-sm">
                            <label class="col-md-3 control-label small">Type Id</label>
                            <div class="col-md-8">
                                <select class="form-control" placeholder="" ng-disabled="viewOnly"
                                        ng-change="selectContactType(dataItem.CustomerContactTypeId)"
                                        ng-model="customerContactPhone.ContactNumberTypeId" ng-options="cCon.Id as cCon.Type for cCon in CustomerContactTypes"></select>
                            </div>
                        </div>
                    </form>
                </div>

            </div>
        </div>
        <div class="panel-footer text-right">
            <button class="btn btn-primary" type="submit"
                    ng-click="saveCustomerContactPHone(ContatPhoneAction)">
                {{ContatPhoneAction === 'Create' ? 'Add' : ContatPhoneAction}}
            </button>

        </div>
    </div>
</div>

<!-- Admin Form Popup -->
<div id="modal-panel" class="popup-basic bg-none mfp-with-anim mfp-hide">
    <!-- begin: .panel -->
    <div class="panel">
        <div class="panel-heading">
            <span class="panel-title"><i class="glyphicon glyphicon-tasks"></i>{{actionMode + " " + modelName}}</span>
        </div>
        <!-- end .panel-heading section -->
        <!-- Panel Body -->
        <div class="panel-body">
            <div class="alert alert-danger alert-sm" ng-show="isError">
                <i class="fa fa-remove pr10"></i>
                <strong>ERROR!</strong>    {{errorMessage}}
            </div>
        </div>
        <!-- Panel Footer -->
        <div class="panel-footer text-right">
            <button class="btn btn-primary" type="submit" ng-click="submit()">{{submitButtonText}}</button>
        </div>
    </div>
    <!-- end: .panel -->
</div>
<!-- end: .admin-form -->

